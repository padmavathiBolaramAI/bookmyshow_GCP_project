[databases]
booking = host=booking-db private_ip dbname=booking user=booking_user

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = md5
pool_mode = transaction
max_client_conn = 1000
default_pool_size = 100
